# Fixed requirements v2 - Resolves Pillow conflicts
# Install in this exact order to avoid conflicts

# CRITICAL: Pin Pillow version that works with all packages
Pillow==10.4.0  # Works with gradio, matplotlib, and older mineru

# Core dependencies (install first)
python-dotenv==1.1.1
numpy>=2.0.0

# Gradio (before matplotlib to avoid conflicts)
gradio==4.44.0
gradio-client==1.3.0

# Matplotlib (specific version)
matplotlib==3.7.2

# Document Processing
# Note: mineru might need adjustment for Pillow compatibility
pypdf==4.3.1
python-docx==1.1.2
markdown==3.6
pandas==2.3.1
openpyxl==3.1.5
tqdm==4.67.1

# MinerU - install without strict Pillow requirement
# We'll handle this specially in install script
# mineru[core]==2.1.10

# LightRAG
lightrag-hku>=1.4.5

# Azure OpenAI
openai==1.50.0
azure-identity==1.17.1
azure-core==1.30.2

# Google Gemini
google-generativeai>=0.8.0

# Utilities
aiohttp>=3.8.0
tenacity>=8.2.0
tiktoken>=0.5.0
networkx>=3.0
pydantic>=2.0
nest_asyncio>=1.5.0

# Additional dependencies
protobuf>=4.0
grpcio>=1.50.0
typing-extensions>=4.0
huggingface_hub